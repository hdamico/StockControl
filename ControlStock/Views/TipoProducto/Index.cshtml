@model IEnumerable<ControlStock.Models.TipoProducto>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TipoProductoID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoProductoID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.TipoProductoID }) |
                @Html.ActionLink("Details", "Details", new { id=item.TipoProductoID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.TipoProductoID })
            </td>
        </tr>
}
    </tbody>
</table>
